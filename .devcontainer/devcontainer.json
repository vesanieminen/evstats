{
  "name": "Java Agents",
  "image": "mcr.microsoft.com/devcontainers/java:21-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "lts"
    }
  },
  "postCreateCommand": "curl -fsSL https://claude.ai/install.sh | bash && npm install -g @openai/codex @playwright/cli@latest && playwright-cli install --skills && ~/.local/bin/claude mcp add vaadin --transport http https://mcp.vaadin.com/docs && npm install && npx playwright install --with-deps chromium",
  "remoteEnv": {
    "PATH": "${containerEnv:HOME}/.local/bin:${containerEnv:PATH}"
  },
  "appPort": [
    "8080:8080",
    "35729:35729"
  ]
}
